1. Инициализация angular приложения.
2. Установка и настройка eslint(airbnb), prettier.
3. Установка библиотеки ngrx(store, effects, router-store, store-devtools, store-schematics)
4. Примерная структура приложения:
  app
    -core
      -components
        -header
        -footer
        -page-not-found(страница ошибки 404)
      -services
      -guards
      -interceptor
      -models
      -constants
    -shared
      -components
      -directives
      -pipes
    -main
      -pages
        -results-page(страница с результатами поиска(select ticket))
        -search-page(главная страница с поиском)
      -components
    -booking
      -pages
        -booking-page(страница бронирования)
        -summary-page(Сводная страница (Summary))
      -components
    -cart(страница корзины и возможно страница user account)
      -pages
        -cart-page(страница корзины)
      -components
    -styles(папка с глобальными стилями)
      -variables.scss(глобальные переменные)
  assets
    -icons
    -images
  docs(папка с описанием репа и т.п.)

5. Настройка для деплоя на netlify (файл: _redirects).
6. Изменил favicon.ico
7. Добавил normalize.css
